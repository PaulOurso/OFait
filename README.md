# O'Fait
### Version 1.0.0


Init
==================
1. Change directory to "/server"
2. `npm install`

Run server
==================
1. Change directory to "/server"
2. Run server (choice):
`npm run serve` (**REQUIRE nodemon installed !!**) or `node .`